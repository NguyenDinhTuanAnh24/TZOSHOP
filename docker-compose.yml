services:
  tzoshop:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DATABASE_URL: ${DATABASE_URL}
        DIRECT_URL: ${DIRECT_URL}
    container_name: tzoshop
    ports:
      - "3004:3004"
    env_file:
      - .env
    restart: unless-stopped
